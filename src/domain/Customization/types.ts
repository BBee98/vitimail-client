import type {IconType} from "react-icons";

export type CustomizationOptionsProps = {
    canPadding?: boolean,
    canBackground?: boolean,
    canColor?: boolean,
    canFontSize?: boolean,
}


export type CustomizationProps = {
    icon: IconType
}